import { Element } from '$lib/types';
import { Themes } from '$lib/providers';

export { default } from './Header';

export interface ThemeToggleProps extends Element<'button'> {}
export interface ThemeGraphicProps extends Element<'svg'> {}
